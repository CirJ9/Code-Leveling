/**
 * Visual style constants for the battle menu UI (text styles, hover styles, etc.)
 */
import Phaser from "../../../../lib/phaser.js";

/** @type {Phaser.Types.GameObjects.Text.TextStyle} */
export const BATTLE_UI_TEXT_STYLE = Object.freeze({
    
        fontFamily: 'FutureNarrow',
        fontSize: '20px',
        color: '#ffffffff',
    });

/** @type {Phaser.Types.GameObjects.Text.TextStyle} */
export const CHOICE_HOVER = Object.freeze({
    
        color: '#1ccaffff',
    });
